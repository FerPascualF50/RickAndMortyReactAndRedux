import React from "react";
import error from '../assets/error.png'

const Error = () => (
  <div>
    <img src={error} alt=" Morty SCARED" />
    <span>Ups, an error has ocurred.</span>
  </div>
);

export default Error;