import React from "react";
import portal from '../assets/portal.gif';

const Loader = () => (
  <div>
    <img src={portal} alt=" Loadinggggg..." />
    <span>Ups, an error has ocurred.</span>
  </div>
);

export default Loader;